# CPU Baseline Graphs

This folder is for graphs made by the pure Python script.

* **cpu_runtime.png**: This is a simple line graph that shows how long the Python CPU code takes to run as the sequence length increases.

It is made when you run the `src/attention_cpu.py` file.
